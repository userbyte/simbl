## setup documentation is WIP

...
[insert next.js app initial setup instructions]
...

then, set the `JWT_SECRET` environment variable to something super incredibly secret (required)

**.env**
```
JWT_SECRET="something_secret"
```